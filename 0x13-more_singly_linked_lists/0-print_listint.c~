#include "lists.h"

/**
* print_listint - prints all the elements of a linked list
* @s: linked list of type listint_t to print
*
* Return: number of nodes
*/
size_t print_listint(const listint_t *s)
{
size_t number = 0;

while (s)
{
printf("%d\n", s->n);
num++;
s = s->next;
}
return (number);
}
