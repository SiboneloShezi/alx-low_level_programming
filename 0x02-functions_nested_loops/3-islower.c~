#include "main.h"


/**
* _islower - check if char is lowercase
* @c: is the charecter that will be checked
* Return: 1 if char is lowercase, otherwise 0
*/


int _islower(int c)
{
  return (c >= 'a' && c <= 'z');
}
